---
phase: 05.2-milestone-feedbacks
plan: 02
subsystem: domain
tags: [refresh, staleness, yarn-integrity, command-lifecycle]

requires:
  - phase: 05.2-01
    provides: "UI polish (borders, titles, table restructure)"
provides:
  - "RefreshSet struct and refresh_needed() pure domain function"
  - "CommandExited handler wired to refresh_needed()"
  - "check_stale() using .yarn-integrity sentinel"
affects: [command-lifecycle, worktree-staleness]

tech-stack:
  added: []
  patterns: [data-dependency-model, single-source-of-truth-refresh]

key-files:
  created: [src/domain/refresh.rs]
  modified: [src/domain/mod.rs, src/app.rs, src/infra/worktrees.rs]

key-decisions:
  - "RefreshSet as plain struct with bool fields — simple, exhaustive, testable"
  - "Worktree reload implicitly handles JIRA re-fetch via existing WorktreesLoaded handler"
  - ".yarn-integrity sentinel replaces node_modules dir mtime for reliable staleness"

patterns-established:
  - "Data dependency model: pure function maps command -> required refreshes"
  - "CommandExited uses .take() to capture completed command before clearing"

requirements-completed: [FIX-02, FIX-03]

duration: 2min
completed: 2026-03-09
---

# Phase 05.2 Plan 02: Data Dependency Model and Staleness Fix Summary

**Pure domain refresh_needed() function mapping CommandSpec to RefreshSet, wired into CommandExited with .yarn-integrity staleness sentinel**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-09T07:57:55Z
- **Completed:** 2026-03-09T07:59:48Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Created RefreshSet struct and refresh_needed() pure domain function with 20 tests
- Wired refresh_needed() into CommandExited handler as single source of truth for post-command refreshes
- Rewrote check_stale() to use .yarn-integrity sentinel file instead of node_modules dir mtime

## Task Commits

Each task was committed atomically:

1. **Task 1: Create refresh_needed() domain function** - `b2709d3` (feat) — TDD: tests + implementation in single commit
2. **Task 2: Wire refresh into CommandExited and rewrite check_stale()** - `6f5186e` (feat)

## Files Created/Modified
- `src/domain/refresh.rs` - RefreshSet struct and refresh_needed() pure domain function with tests
- `src/domain/mod.rs` - Added pub mod refresh
- `src/app.rs` - CommandExited handler calls refresh_needed() and dispatches appropriate actions
- `src/infra/worktrees.rs` - check_stale() rewritten to use .yarn-integrity sentinel

## Decisions Made
- RefreshSet as plain struct with bool fields — simple, exhaustive, testable (no bitflags needed)
- Worktree reload implicitly handles JIRA title re-fetch via existing WorktreesLoaded handler — no additional wiring needed
- .yarn-integrity sentinel replaces node_modules dir mtime — written by yarn on every successful install, eliminates false positives from node_modules subdirectory changes

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Refresh data dependency model in place for all future command additions
- Staleness detection is reliable via .yarn-integrity sentinel
- Ready for remaining 05.2 plans

---
*Phase: 05.2-milestone-feedbacks*
*Completed: 2026-03-09*
