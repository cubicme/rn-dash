---
phase: 05.2-milestone-feedbacks
plan: 03
type: execute
wave: 3
depends_on: ["05.2-02"]
files_modified:
  - src/app.rs
  - src/action.rs
  - src/ui/panels.rs
  - src/infra/devices.rs
autonomous: true
requirements:
  - FIX-05
  - FIX-06

must_haves:
  truths:
    - "hjkl scroll works in both metro pane and command output pane"
    - "G scrolls to bottom in any scrollable pane"
    - "gg (two g presses) scrolls to top in any scrollable pane"
    - "Metro log auto-follows tail when user has not manually scrolled up"
    - "Manual scroll up disables auto-follow; G re-enables it"
    - "Tab while in fullscreen exits fullscreen instead of cycling focus"
    - "iOS device picker (i>d) shows physical devices, not simulators"
    - "Metro debugger (j key when metro pane focused) sends debugger command to metro stdin"
  artifacts:
    - path: "src/app.rs"
      provides: "Universal scroll keybindings, auto_follow flag, fullscreen Tab exit, debugger fix"
      contains: "auto_follow"
    - path: "src/action.rs"
      provides: "ScrollToTop, ScrollToBottom action variants"
  key_links:
    - from: "src/app.rs handle_key()"
      to: "Action::ScrollToTop/ScrollToBottom"
      via: "G and gg keybindings in scrollable panes"
      pattern: "ScrollTo(Top|Bottom)"
    - from: "src/app.rs handle_key()"
      to: "Action::ToggleFullscreen"
      via: "Tab key when fullscreen_panel.is_some()"
      pattern: "fullscreen_panel.*Tab"
---

<objective>
Navigation enhancements and bug fixes: universal hjkl/G/gg scrolling in all scrollable panes, metro log auto-follow with manual scroll override, fullscreen Tab exit, iOS device picker fix, and metro debugger fix.

Purpose: Make vim-style scrolling work everywhere consistently, fix auto-scroll behavior, and resolve three reported bugs (fullscreen Tab, iOS picker, debugger).
Output: Updated handle_key() with universal scroll, new auto_follow state, fixed device enumeration.
</objective>

<execution_context>
@/Users/cubicme/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cubicme/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-milestone-feedbacks/05.2-CONTEXT.md
@.planning/phases/05.2-milestone-feedbacks/05.2-RESEARCH.md
@.planning/phases/05.2-milestone-feedbacks/05.2-01-SUMMARY.md
@.planning/phases/05.2-milestone-feedbacks/05.2-02-SUMMARY.md

@src/app.rs
@src/action.rs
@src/infra/devices.rs

<interfaces>
From src/app.rs (current scroll handling in MetroPane):
```rust
// line ~339: if state.focused_panel == FocusedPanel::MetroPane
Char('k') => return Some(Action::MetroScrollUp),
Char('j') | Down => return Some(Action::MetroScrollDown),
Char('J') => return Some(Action::MetroSendDebugger),
```

From src/app.rs (current scroll handling in CommandOutput):
```rust
// line ~375: if state.focused_panel == FocusedPanel::CommandOutput
Char('j') | Down => return Some(Action::FocusDown),  // NOTE: this is wrong, should be scroll
Char('k') | Up => return Some(Action::FocusUp),      // NOTE: this is wrong, should be scroll
```

From src/app.rs (current normal mode):
```rust
Tab => Some(Action::FocusNext),  // line ~395 — no fullscreen check
```

From src/app.rs AppState:
```rust
pub log_scroll_offset: usize,
pub command_output_scroll_by_worktree: HashMap<WorktreeId, usize>,
```

From src/infra/devices.rs:
```rust
pub async fn list_ios_devices() -> anyhow::Result<Vec<DeviceInfo>> {
    // Currently runs: xcrun simctl list devices available --json
    // This returns SIMULATORS, not physical devices
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Universal scroll keybindings and auto-follow</name>
  <files>src/app.rs, src/action.rs</files>
  <action>
  1. **Add new Action variants** to `src/action.rs`:
     - `ScrollToTop` — gg (go to top of scrollable pane)
     - `ScrollToBottom` — G (go to bottom, re-enable auto-follow)

  2. **Add `metro_log_auto_follow: bool` and `pending_g: bool` to AppState** in `src/app.rs`:
     - `metro_log_auto_follow: bool` — default `true`. When true, metro log pane auto-scrolls to bottom on new content. Set to `false` when user scrolls up. Set back to `true` on `ScrollToBottom` / `G`.
     - `pending_g: bool` — default `false`. For `gg` two-key sequence: first `g` sets `pending_g = true`, second `g` triggers `ScrollToTop`, any other key clears it.

  3. **Fullscreen Tab exit** — In `handle_key()`, BEFORE the `// --- NORMAL MODE ---` section (before line ~386), add:
     ```rust
     // Fullscreen: Tab exits fullscreen
     if state.fullscreen_panel.is_some() {
         if key.code == Tab {
             return Some(Action::ToggleFullscreen);
         }
     }
     ```

  4. **Universal scroll in MetroPane** — Update the MetroPane key handling (line ~339):
     - `Char('j')` -> `MetroScrollDown` (already correct)
     - `Char('k')` -> `MetroScrollUp` (already correct)
     - `Char('J')` -> `MetroSendDebugger` (keep as-is — Shift-J sends debugger)
     - ADD `Char('G')` -> `ScrollToBottom`
     - ADD `Char('g')` -> check `pending_g`: if true return `ScrollToTop`, else set `pending_g = true` and return `None`. BUT note: `g` currently maps to `EnterGitPalette` in WorktreeTable panel. In MetroPane, `g` is free. Just need the pending_g logic.

  5. **Universal scroll in CommandOutput** — Update the CommandOutput key handling (line ~375):
     - Fix `Char('j')` to dispatch a command output scroll down action (currently dispatches FocusDown which is wrong). Use existing `Action::FocusDown` repurposed or add `CommandOutputScrollDown`/`CommandOutputScrollUp`. Looking at existing code, there are no dedicated command output scroll actions. Add them:
       - Add `CommandOutputScrollUp` and `CommandOutputScrollDown` to Action enum.
       - In update(), handle them by adjusting the active worktree's scroll offset in `command_output_scroll_by_worktree`.
     - ADD `Char('G')` -> `ScrollToBottom`
     - ADD `Char('g')` -> same `pending_g` logic as MetroPane

  6. **Handle ScrollToTop and ScrollToBottom in update()**:
     - `ScrollToTop`: if focused on MetroPane, set `log_scroll_offset = 0` (but NOT auto_follow — user is scrolling to top manually, so `metro_log_auto_follow = false`). Wait — actually, `log_scroll_offset = 0` currently means "auto-scroll to bottom" in the rendering code. This is the root bug. We need to fix the semantics:
       - With `auto_follow` flag, rendering should check `auto_follow` for tail behavior, not `log_scroll_offset == 0`.
       - `ScrollToTop` sets `log_scroll_offset = 0` and `auto_follow = false`.
       - `ScrollToBottom` sets `log_scroll_offset = max_scroll` and `auto_follow = true`.
       - `MetroScrollUp` sets `auto_follow = false` and decrements offset.
       - `MetroScrollDown` increments offset (does NOT set auto_follow = true; only G does).
       - Same pattern for command output (using per-worktree scroll and a per-worktree auto_follow or a simpler approach — command output is not continuous stream so auto_follow is less critical; just implement G/gg).

  7. **Update metro log rendering in panels.rs** (from Plan 01's work): The auto-scroll logic in `render_metro_pane()` currently checks `log_scroll_offset == 0`. Change it to check `state.metro_log_auto_follow`:
     ```rust
     let scroll = if state.metro_log_auto_follow && !lines.is_empty() {
         lines.len().saturating_sub(visible_height)
     } else {
         state.log_scroll_offset
     };
     ```

  8. **Clear pending_g on any non-g key**: At the start of handle_key(), if `state.pending_g` is true and key is not `Char('g')`, set `state.pending_g = false`. Important: handle_key takes `&AppState` (immutable). The pending_g clearing must happen in update() via a new action, OR we change handle_key to take `&mut AppState`. Looking at existing code, handle_key takes `&AppState`. So: return a new `Action::ClearPendingG` or handle it differently. Simpler approach: skip the two-key `gg` sequence entirely. Use single `g` for scroll-to-top (this is a simplification the research suggested as an option). Per CONTEXT.md: "hjkl, G, gg work everywhere" — user wants gg. But since handle_key is pure, we need the pending_g approach via actions.

     Alternative: Add a `pending_g` check at the TOP of handle_key. If `pending_g` is true:
     - If `Char('g')` -> return `Some(Action::ScrollToTop)` (and update() clears pending_g)
     - Otherwise -> handle_key proceeds normally, but we need to clear pending_g. Since handle_key returns Option<Action>, and the action it returns might be something else, we need a way to also clear pending_g. Solution: in update(), EVERY action clears pending_g EXCEPT when the action is... no, that's messy.

     Best approach: Make `pending_g` work through the action system:
     - `Char('g')` in scrollable pane: if `state.pending_g` -> return `ScrollToTop`. Else return `Some(Action::SetPendingG)`.
     - `Action::SetPendingG` in update(): `state.pending_g = true`.
     - `Action::ScrollToTop` in update(): sets scroll to 0, sets `pending_g = false`.
     - At the START of update(), for EVERY action except SetPendingG, clear pending_g. This is one line: `if !matches!(action, Action::SetPendingG) { state.pending_g = false; }`.
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Tab exits fullscreen. G/gg scroll to bottom/top in MetroPane and CommandOutput. Metro log auto-follows tail (auto_follow flag). Manual scroll up disables auto-follow. j/k scroll in command output pane (not FocusDown/Up). pending_g clears on any non-g action.</done>
</task>

<task type="auto">
  <name>Task 2: iOS device picker fix and metro debugger verification</name>
  <files>src/infra/devices.rs, src/app.rs</files>
  <action>
  1. **iOS device picker fix** — The `list_ios_devices()` function in `src/infra/devices.rs` uses `xcrun simctl list devices` which returns simulators, not physical devices. The CONTEXT.md says "iOS device picker (i>d) shows simulators instead of physical devices — fix device enumeration filtering."

     Looking at the palette modes: `EnterIosPalette` shows options including `d` for device. The issue is that `i>d` should show physical devices but shows simulators. Fix:
     - Add a new function `list_ios_physical_devices()` that uses `xcrun devicectl list devices` (Xcode 16+) or falls back to `xcrun xctrace list devices` to list connected physical iOS devices. Parse the output for UDID, name, and model.
     - Alternatively, if `xcrun xctrace list devices` is more broadly available: it lists both physical and simulator devices with clear labeling. Physical devices appear first (before "== Simulators ==" header).
     - Keep `list_ios_devices()` as `list_ios_simulators()` (rename for clarity) and add `list_ios_physical_devices()`.
     - In `src/app.rs`, find where `i>d` dispatches device enumeration. It should call `list_ios_physical_devices()` instead of `list_ios_devices()`. The `i>s` (simulator) path should call the renamed `list_ios_simulators()` (formerly `list_ios_devices()`).
     - Check the exact palette handling: `EnterIosPalette` in handle_key maps sub-keys. Find the `Char('d')` arm and verify it calls the correct device listing function.

  2. **Metro debugger verification** — The `Char('J')` (Shift-J) binding at line ~345 maps to `MetroSendDebugger`. In update(), `MetroSendDebugger` calls `state.metro.send_stdin(b"j\n".to_vec())`. Verify:
     - The send_stdin() method uses `self.handle.as_ref()?.stdin_tx.send(bytes)` — this should work if metro is running and the stdin channel is open.
     - The bug report says "j key not working" — this could mean lowercase j (which maps to MetroScrollDown, not debugger). The user might expect lowercase j to trigger debugger. Check CONTEXT.md: "Metro debugger (j key) not working". The original METRO-07 requirement says "send debugger command (j)". But the current binding is Shift-J to avoid collision with j=scroll.
     - Resolution: Per CONTEXT.md "hjkl, G, gg work everywhere for scrolling — these are universal". So j=scroll is correct. The debugger should remain on Shift-J. The "j key not working" in the bug report likely refers to the stdin pipe not delivering bytes. Check if `send_stdin()` actually writes to the metro process stdin. The stdin_tx channel sends to `stdin_writer()` task which writes to `child.stdin`. Verify this chain is intact. If the chain looks correct, the fix might be that metro needs a specific sequence (not just "j\n"). React Native metro's debugger is triggered by pressing 'j' in the terminal — so `b"j\n"` should work.
     - If after investigation the stdin pipe is working, add a visual feedback: append "[debugger command sent]" to metro_logs when MetroSendDebugger is dispatched, so the user knows the action was received.
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>iOS device picker (i>d) lists physical devices via xcrun xctrace or devicectl. Metro debugger Shift-J sends stdin and shows visual feedback in metro log.</done>
</task>

</tasks>

<verification>
- `cargo check` passes
- handle_key has fullscreen Tab check before normal mode section
- ScrollToTop and ScrollToBottom actions exist and are handled in update()
- metro_log_auto_follow field exists in AppState
- list_ios_physical_devices() function exists in devices.rs
- MetroSendDebugger handler provides visual feedback
</verification>

<success_criteria>
Universal vim-style scrolling (hjkl/G/gg) works in metro pane and command output. Metro log auto-follows with opt-out on manual scroll. Tab exits fullscreen. iOS device picker shows physical devices. Metro debugger is verified working or fixed with visual feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-milestone-feedbacks/05.2-03-SUMMARY.md`
</output>
