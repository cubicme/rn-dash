---
status: resolved
phase: 05.2-milestone-feedbacks
source: [05.2-01-SUMMARY.md, 05.2-02-SUMMARY.md, 05.2-03-SUMMARY.md, 05.2-04-SUMMARY.md]
started: 2026-03-09T08:15:00Z
updated: 2026-03-11T00:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Double-Line Borders
expected: |
  All four panes (metro, log, output, worktrees) display double-line borders (║ ═) instead of single-line borders.
awaiting: user response

## Tests

### 1. Double-Line Borders
expected: All four panes (metro, log, output, worktrees) display double-line borders (║ ═) instead of single-line borders.
result: pass

### 2. Dynamic Metro Pane Title
expected: Metro pane title shows live status (e.g. "Metro ─ RUNNING ─ worktree-name" or "Metro ─ STOPPED"). Status updates when metro starts/stops.
result: pass

### 3. Worktree Table Columns
expected: Worktree table has exactly 4 columns: Status | Label | Branch | Ticket. No separate "Name" column. Ticket column shows number + title merged.
result: issue
reported: "always show Y (green=fresh, red=stale) for yarn and  or P (green/red) for pods. Also yarn install doesn't update list until restart — state change should trigger refresh"
severity: major

### 4. Metro-Active Row Highlight
expected: The worktree row that has metro running is visually highlighted with bold text and a darker background, distinguishing it from other rows.
result: issue
reported: "youtube row is running metro but has no visual distinction from other rows — green bold styling not applied"
severity: major

### 5. Post-Command Auto-Refresh
expected: After a command finishes (e.g. yarn install, pod install), the worktree list automatically refreshes without manual intervention. Staleness indicator updates accordingly.
result: issue
reported: "still falsely stale — staleness fix from plan 06 not working, also yarn install doesn't trigger list refresh"
severity: major

### 6. Staleness Detection
expected: A worktree shows as stale when its .yarn-integrity file is missing or outdated relative to yarn.lock. Running yarn install clears the stale indicator.
result: issue
reported: "still falsely stale on all worktrees — multi-sentinel check_stale() still broken"
severity: major

### 7. Vim-Style G/gg Scrolling
expected: In metro pane and command output: pressing G jumps to bottom, pressing gg (two g's) jumps to top. j/k scroll one line at a time.
result: pass

### 8. Metro Auto-Follow
expected: Metro log automatically scrolls to show new lines as they arrive. Scrolling up manually pauses auto-follow. Pressing G re-enables auto-follow.
result: pass

### 9. Tab Exits Fullscreen
expected: If a pane is in fullscreen mode, pressing Tab first exits fullscreen and then cycles focus to the next pane (does not stay fullscreen on a different pane).
result: pass

### 10. Command Output j/k Scroll
expected: In the command output pane, j scrolls down and k scrolls up (line by line). Previously these were incorrectly changing focus instead of scrolling.
result: pass

### 11. iOS Physical Device Picker
expected: Pressing i then d shows a list of physical iOS devices connected via USB (from xctrace), not simulators. Simulators appear in a separate list.
result: pass
note: "doesn't show a list picker but runs on the device directly — user says they don't need a list"

### 12. Metro Debugger Feedback
expected: Pressing Shift-J to toggle metro debugger shows visual feedback in the metro log confirming success or failure of the debugger toggle.
result: issue
reported: "still not working. It's lowercase j sent to metro stdin, may prompt for numeric input (1-9). Need to understand how metro's j command actually works before fixing."
severity: major

### 13. External Metro Detection on Startup
expected: If an external metro process is already running on port 8081 when the dashboard launches, a conflict modal appears showing the PID and working directory of the external process.
result: pass

### 14. External Metro Conflict Modal
expected: The conflict modal has double-line borders and shows PID + working directory. Offers "Y" to kill the external process or "N/Esc" to cancel.
result: pass

### 15. Kill-and-Auto-Start Flow
expected: Pressing Y in the conflict modal kills the external metro process and automatically starts the dashboard's own metro bundler after a brief delay.
result: pass

## Summary

total: 15
passed: 10
issues: 7
pending: 0
skipped: 0

## Gaps

### Resolved (round 2)
- Test 2: Pane title readability — FIXED (white titles)
- Test 7: G scroll scrollbar position — FIXED
- Test 9: Tab fullscreen cycle — FIXED

### Resolved (round 3 — plans 08-10)

- truth: "Status icons: always show Y (green=fresh, red=stale) for yarn, /P for pods. Always visible."
  status: resolved
  resolution: "Plan 08 Task 2 — letter-based Y/P icons always visible"
  test: 3

- truth: "Metro-active worktree row visually highlighted"
  status: resolved
  resolution: "Plan 08 Task 1 — WorktreesLoaded derives metro_status from MetroManager"
  test: 4

- truth: "Staleness detection accurate — not falsely stale"
  status: resolved
  resolution: "Plan 09 Task 1 — check_stale uses .yarn/install-state.gz (Berry) + .yarn-integrity (v1)"
  test: 5+6

- truth: "Post-command refresh updates worktree list live"
  status: resolved
  resolution: "Plan 09 Task 1 — staleness root cause was check_stale(), refresh wiring was already correct"
  test: 5

- truth: "Metro debugger toggle works via lowercase j to metro stdin"
  status: resolved
  resolution: "Plan 09 Task 2 — sends j keystroke to metro stdin"
  test: 12

- truth: "Worktree table shows a Dir column at the end displaying the worktree directory"
  status: resolved
  resolution: "Plan 08 Task 2 — Dir column with worktree directory basename"
  test: user-requested

- truth: "When a label is set, use it as the display name everywhere — claude tab name, pane title, etc. A single domain function returns the preferred prefix (label > ticket > branch > workspace name)"
  status: resolved
  resolution: "Plan 10 — preferred_prefix() domain method wired into Claude tabs and metro title"
  test: user-requested
