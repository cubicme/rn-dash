---
phase: 05.2-milestone-feedbacks
verified: 2026-03-09T08:30:00Z
status: passed
score: 20/20 must-haves verified
re_verification: false
---

# Phase 05.2: Milestone Feedbacks Verification Report

**Phase Goal:** Bug fixes and UX polish from real usage -- double-line border visual overhaul, dynamic pane titles, table column restructuring, data dependency model (refresh_needed), staleness detection rewrite (.yarn-integrity), universal vim scrolling (hjkl/G/gg), metro auto-follow, external metro conflict detection, and bug fixes (iOS device picker, metro highlight, debugger, fullscreen Tab)
**Verified:** 2026-03-09T08:30:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All panes have double-line borders (BorderType::Double) | VERIFIED | 4 occurrences of `BorderType::Double` in panels.rs (metro, log, output, worktrees) |
| 2 | Metro pane title is dynamic: running (worktree-name) or stopped | VERIFIED | panels.rs matches on `MetroStatus::Running { worktree_id }` to build dynamic title |
| 3 | Output pane title shows queue count | VERIFIED | panels.rs formats `" Output ({} queued) "` when queue non-empty |
| 4 | Worktree table has titled border (Worktrees) | VERIFIED | panels.rs: `.title(" Worktrees ")` on worktree table block |
| 5 | Standalone RUNNING text removed from metro pane body | VERIFIED | No `RUNNING [` or `status_text` or `status_line` patterns in panels.rs |
| 6 | Table columns are Status, Label, Branch, Ticket (4 columns) | VERIFIED | Exactly 4 Constraint entries: Length(4), Length(14), Length(20), Min(30) |
| 7 | Metro-active worktree row highlighted with bold + DarkGray background | VERIFIED | `WorktreeMetroStatus::Running` => `fg(Color::White).bg(Color::DarkGray).add_modifier(Modifier::BOLD)` |
| 8 | CommandExited triggers appropriate refreshes based on completed command type | VERIFIED | app.rs calls `crate::domain::refresh::refresh_needed(cmd)` in CommandExited arm |
| 9 | Branch-changing git ops trigger full worktree reload | VERIFIED | refresh.rs: GitCheckout/GitCheckoutNew/GitRebase/GitResetHard/GitResetHardFetch => worktrees:true, staleness:true, jira_titles:true. 20 tests pass. |
| 10 | Non-branch-changing git ops do NOT trigger worktree reload | VERIFIED | refresh.rs: GitPull/GitPush/GitFetch => RefreshSet::none(). Tests confirm. |
| 11 | Install/clean commands trigger staleness refresh only | VERIFIED | refresh.rs: YarnInstall/YarnPodInstall/RmNodeModules/RnCleanAndroid/RnCleanCocoapods => staleness:true only. Tests confirm. |
| 12 | Staleness detection uses .yarn-integrity file | VERIFIED | worktrees.rs: `check_stale()` uses `node_modules/.yarn-integrity` as sentinel, with mtime comparison against package.json/yarn.lock |
| 13 | hjkl scroll works in metro pane and command output pane | VERIFIED | j/k dispatch MetroScrollDown/Up in MetroPane; CommandOutputScrollDown/Up in CommandOutput |
| 14 | G scrolls to bottom, gg scrolls to top in scrollable panes | VERIFIED | ScrollToBottom and ScrollToTop actions in action.rs; G/g keybindings in both MetroPane and CommandOutput sections of handle_key() |
| 15 | Metro log auto-follows tail; manual scroll up disables; G re-enables | VERIFIED | `metro_log_auto_follow` field in AppState (default true), panels.rs checks flag for scroll position, MetroScrollUp sets false, ScrollToBottom sets true |
| 16 | Tab while in fullscreen exits fullscreen | VERIFIED | app.rs: `if state.fullscreen_panel.is_some() { if key.code == Tab { return Some(Action::ToggleFullscreen); } }` before normal mode section |
| 17 | iOS device picker shows physical devices, not simulators | VERIFIED | devices.rs: `list_ios_physical_devices()` uses `xcrun xctrace list devices`, `parse_xctrace_devices()` parses physical devices before Simulators header |
| 18 | Metro debugger sends command and shows visual feedback | VERIFIED | app.rs: MetroSendDebugger sends `b"j\n"`, pushes `[debugger command sent]` to metro_logs on success, `[debugger send failed: {e}]` on error |
| 19 | Dashboard checks port 8081 on startup for external metro | VERIFIED | app.rs run(): spawns `detect_external_metro(8081)` on startup, dispatches ExternalMetroDetected |
| 20 | Dashboard checks port 8081 before every metro start; shows prompt with Kill/Cancel | VERIFIED | MetroStart dispatches async detection, routes to ExternalMetroDetected (modal) or MetroStartConfirmed. Modal shows PID + working directory with Y=Kill, N/Esc=Cancel. KillExternalMetro kills process and auto-starts metro. |

**Score:** 20/20 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/ui/panels.rs` | Double-line borders, dynamic titles, restructured table, metro highlight | VERIFIED | Contains BorderType::Double (4x), dynamic metro title, 4-column layout, DarkGray highlight |
| `src/domain/refresh.rs` | RefreshSet struct and refresh_needed() function | VERIFIED | Both exported, 20 passing tests |
| `src/domain/mod.rs` | pub mod refresh declaration | VERIFIED | Line 6: `pub mod refresh;` |
| `src/infra/worktrees.rs` | check_stale() using .yarn-integrity | VERIFIED | Uses `node_modules/.yarn-integrity` sentinel |
| `src/app.rs` | Universal scroll, auto_follow, fullscreen Tab, debugger feedback, external metro detection | VERIFIED | All features wired into handle_key() and update() |
| `src/action.rs` | ScrollToTop, ScrollToBottom, SetPendingG, CommandOutputScrollUp/Down, ExternalMetroDetected, KillExternalMetro, MetroStartConfirmed | VERIFIED | All action variants present |
| `src/infra/port.rs` | detect_external_metro(), kill_process(), ExternalMetroInfo | VERIFIED | All functions and struct present |
| `src/infra/devices.rs` | list_ios_physical_devices(), parse_xctrace_devices() | VERIFIED | Both functions present, uses xcrun xctrace |
| `src/domain/command.rs` | ExternalMetroConflict modal variant | VERIFIED | ModalState::ExternalMetroConflict { pid, working_dir } |
| `src/ui/modals.rs` | ExternalMetroConflict modal renderer | VERIFIED | render_external_metro_modal() with PID, directory, Kill/Cancel options, double-line border |
| `src/ui/footer.rs` | Footer hints for conflict modal | VERIFIED | ExternalMetroConflict match arm present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| src/ui/panels.rs | AppState.metro.status | Dynamic metro pane title derived from MetroStatus | WIRED | Match on MetroStatus::Running { worktree_id } to build title string |
| src/app.rs | src/domain/refresh.rs | CommandExited calls refresh_needed() | WIRED | `crate::domain::refresh::refresh_needed(cmd)` in CommandExited handler |
| src/domain/refresh.rs | src/domain/command.rs | refresh_needed takes &CommandSpec | WIRED | `fn refresh_needed(cmd: &CommandSpec) -> RefreshSet` |
| src/app.rs handle_key() | Action::ScrollToTop/ScrollToBottom | G and gg keybindings | WIRED | Both MetroPane and CommandOutput sections handle G and pending_g |
| src/app.rs handle_key() | Action::ToggleFullscreen | Tab when fullscreen | WIRED | Check before normal mode section |
| src/app.rs | src/infra/port.rs | detect_external_metro() called at startup and pre-metro-start | WIRED | Both paths spawn async detection task |
| src/app.rs | Action::ExternalMetroDetected | Async detection result dispatched | WIRED | Detection result routes to modal or MetroStartConfirmed |
| src/ui/modals.rs | ModalState::ExternalMetroConflict | Modal rendering | WIRED | Match arm renders PID, directory, Kill/Cancel |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| FIX-01 | 05.2-01 | Visual overhaul (double-line borders, dynamic titles, table restructure) | SATISFIED | All visual changes verified in panels.rs |
| FIX-02 | 05.2-02 | Data dependency model (refresh_needed) | SATISFIED | RefreshSet + refresh_needed() with 20 tests, wired into CommandExited |
| FIX-03 | 05.2-02 | Staleness detection rewrite (.yarn-integrity) | SATISFIED | check_stale() rewritten to use .yarn-integrity sentinel |
| FIX-04 | 05.2-04 | External metro conflict detection | SATISFIED | Full detection + modal + kill + auto-start flow implemented |
| FIX-05 | 05.2-03 | Universal vim scrolling, auto-follow, fullscreen Tab, iOS picker, debugger | SATISFIED | All features verified in app.rs, action.rs, devices.rs |
| FIX-06 | 05.2-01, 05.2-03 | Metro highlight fix + additional bug fixes | SATISFIED | DarkGray bg + BOLD on metro-active row; debugger feedback; fullscreen Tab exit |

**Note:** FIX-01 through FIX-06 are referenced in ROADMAP.md and plan frontmatter but are not defined in REQUIREMENTS.md. These appear to be ad-hoc bug fix / polish requirements introduced for this milestone feedback phase. All are satisfied by implementation evidence.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | No anti-patterns found |

No TODO, FIXME, PLACEHOLDER, or HACK comments found in any modified files. No stub implementations detected. No empty handlers or placeholder returns.

### Build Verification

- `cargo check` passes (4 warnings -- all in unrelated `src/infra/multiplexer.rs` about unused trait methods)
- `cargo test domain::refresh` passes: 20/20 tests

### Human Verification Required

### 1. Double-line border visual appearance

**Test:** Launch the dashboard and observe all four pane borders
**Expected:** All panes (worktrees, metro, log, output) have double-line (Unicode box-drawing) borders
**Why human:** Visual rendering depends on terminal font and Unicode support

### 2. Metro auto-follow behavior

**Test:** Start metro, observe log scrolling, then scroll up with k, then press G
**Expected:** Auto-follow initially tails log. k scroll up disables auto-follow. G re-enables it.
**Why human:** Requires running metro with live log output to verify scroll behavior

### 3. iOS physical device picker

**Test:** Press i>d with a physical iOS device connected
**Expected:** Shows connected physical device(s), not simulator list
**Why human:** Requires physical iOS device and Xcode toolchain

### 4. External metro conflict modal

**Test:** Start metro externally (e.g., `npx react-native start` in a terminal), then try starting metro from dashboard
**Expected:** Modal shows PID and working directory of external metro, with Kill/Cancel options
**Why human:** Requires external process on port 8081

---

_Verified: 2026-03-09T08:30:00Z_
_Verifier: Claude (gsd-verifier)_
