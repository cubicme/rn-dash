---
phase: 05.2-milestone-feedbacks
verified: 2026-03-10T10:00:00Z
status: passed
score: 23/23 must-haves verified
re_verification:
  previous_status: passed
  previous_score: 20/20
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 05.2: Milestone Feedbacks Verification Report

**Phase Goal:** Bug fixes and UX polish from real usage -- double-line border visual overhaul, dynamic pane titles, table column restructuring, data dependency model (refresh_needed), staleness detection rewrite (.yarn-integrity), universal vim scrolling (hjkl/G/gg), metro auto-follow, external metro conflict detection, and bug fixes (iOS device picker, metro highlight, debugger, fullscreen Tab)
**Verified:** 2026-03-10T10:00:00Z
**Status:** passed
**Re-verification:** Yes -- confirming previous pass + verifying gap closure plans 05/06/07

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All panes have double-line borders (BorderType::Double) | VERIFIED | 4 occurrences in panels.rs (lines 38, 160, 231, 300) + 1 in modals.rs (line 246) |
| 2 | Metro pane title is dynamic: running (worktree-name) or stopped | VERIFIED | panels.rs matches on `MetroStatus::Running { pid: _, worktree_id }` to build dynamic title |
| 3 | Output pane title shows queue count | VERIFIED | panels.rs formats queue count into title when non-empty |
| 4 | Worktree table has titled border (Worktrees) | VERIFIED | panels.rs: `.title(" Worktrees ")` on worktree table block |
| 5 | Standalone RUNNING text removed from metro pane body | VERIFIED | No `RUNNING [` or `status_text` patterns in panels.rs |
| 6 | Table columns are Status, Label, Branch, Ticket (4 columns) | VERIFIED | 4 Constraint entries: Length(6), Length(14), Length(20), Min(30) |
| 7 | Metro-active worktree row highlighted with green bold | VERIFIED | `WorktreeMetroStatus::Running` => `fg(Color::Green).add_modifier(Modifier::BOLD)` (panels.rs:82-85) |
| 8 | CommandExited triggers appropriate refreshes based on command type | VERIFIED | app.rs:921 calls `crate::domain::refresh::refresh_needed(cmd)` |
| 9 | Branch-changing git ops trigger full worktree reload | VERIFIED | refresh.rs: GitCheckout/GitCheckoutNew/GitRebase/GitResetHard/GitResetHardFetch => worktrees+staleness+jira. 20 tests pass. |
| 10 | Non-branch-changing git ops do NOT trigger worktree reload | VERIFIED | refresh.rs: GitPull/GitPush/GitFetch => RefreshSet::none(). Tests confirm. |
| 11 | Install/clean commands trigger staleness refresh only | VERIFIED | refresh.rs: YarnInstall/YarnPodInstall/RmNodeModules/RnCleanAndroid/RnCleanCocoapods => staleness:true only |
| 12 | Staleness detection uses multi-sentinel approach (.yarn-integrity + .yarn-state) | VERIFIED | worktrees.rs: checks `.yarn-integrity` (v1) and `.yarn-state` (berry) with mtime comparison |
| 13 | hjkl scroll works in metro pane and command output pane | VERIFIED | j/k dispatch MetroScrollDown/Up in MetroPane; CommandOutputScrollDown/Up in CommandOutput |
| 14 | G scrolls to bottom, gg scrolls to top in scrollable panes | VERIFIED | ScrollToBottom and ScrollToTop actions; G/g keybindings in both MetroPane and CommandOutput |
| 15 | Metro log auto-follows tail; manual scroll up disables; G re-enables | VERIFIED | `metro_log_auto_follow` field default true, scroll up sets false, ScrollToBottom sets true |
| 16 | Tab while in fullscreen exits fullscreen | VERIFIED | app.rs:354: `if state.fullscreen_panel.is_some() { if key.code == Tab { return Some(Action::ToggleFullscreen); } }` |
| 17 | iOS device picker shows physical devices, not simulators | VERIFIED | devices.rs: `list_ios_physical_devices()` uses `xcrun xctrace list devices`, parses before Simulators header |
| 18 | Metro debugger sends 'd' command and shows visual feedback | VERIFIED | app.rs:596: sends `b"d\n"`, pushes `[debugger toggle sent (d)]` on success, error message on failure |
| 19 | Dashboard checks port 8081 on startup for external metro | VERIFIED | app.rs:557: spawns `detect_external_metro(8081)` on startup |
| 20 | Dashboard checks port 8081 before every metro start; shows prompt with Kill/Cancel | VERIFIED | app.rs:1556: MetroStart dispatches detection, routes to ExternalMetroDetected (modal) or MetroStartConfirmed |
| 21 | Pane titles are white text regardless of focus state | VERIFIED | 4x `title_style(Style::default().fg(Color::White))` in panels.rs (lines 40, 162, 233, 302) |
| 22 | Status column shows separate icons for yarn and pods staleness | VERIFIED | `stale_pods` field on Worktree, separate icon span in panels.rs:77, `check_stale_pods()` in worktrees.rs |
| 23 | Scrollbar position correctly tracks content position | VERIFIED | `ScrollbarState::new(max_scroll).position(scroll.min(max_scroll))` in 3 panes (panels.rs:195, 243, 314) |

**Score:** 23/23 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/ui/panels.rs` | Borders, titles, table, highlight, scrollbar | VERIFIED | BorderType::Double (4x), title_style(White) (4x), 4-column layout, green bold metro row, ScrollbarState(max_scroll) |
| `src/domain/refresh.rs` | RefreshSet struct and refresh_needed() | VERIFIED | Both exported, `pub mod refresh` in domain/mod.rs, 20 passing tests |
| `src/infra/worktrees.rs` | check_stale() multi-sentinel, check_stale_pods() | VERIFIED | .yarn-integrity + .yarn-state sentinels, separate pods staleness check |
| `src/domain/worktree.rs` | stale_pods field | VERIFIED | `pub stale_pods: bool` at line 41 |
| `src/app.rs` | Scroll, auto_follow, fullscreen Tab, debugger, external metro, staleness refresh | VERIFIED | All features wired into handle_key() and update() |
| `src/action.rs` | ScrollToTop, ScrollToBottom, MetroSendDebugger, ExternalMetroDetected, KillExternalMetro | VERIFIED | All action variants present |
| `src/infra/port.rs` | detect_external_metro(), kill_process(), ExternalMetroInfo | VERIFIED | All functions and struct present, no TODOs |
| `src/infra/devices.rs` | list_ios_physical_devices(), parse_xctrace_devices() | VERIFIED | Both functions present |
| `src/domain/command.rs` | ExternalMetroConflict modal variant | VERIFIED | ModalState::ExternalMetroConflict { pid, working_dir } |
| `src/ui/modals.rs` | ExternalMetroConflict modal renderer | VERIFIED | render_external_metro_modal() with double-line border |
| `src/ui/footer.rs` | Footer hints for conflict modal | VERIFIED | ExternalMetroConflict match arm present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| panels.rs | AppState.metro.status | Dynamic metro title | WIRED | Match on MetroStatus::Running to build title |
| app.rs | domain/refresh.rs | CommandExited calls refresh_needed() | WIRED | Line 921 |
| refresh.rs | domain/command.rs | refresh_needed takes &CommandSpec | WIRED | Function signature confirmed |
| app.rs | Action::ScrollToTop/ScrollToBottom | G and gg keybindings | WIRED | Both MetroPane and CommandOutput |
| app.rs | Action::ToggleFullscreen | Tab when fullscreen | WIRED | Line 354 check before normal mode |
| app.rs | infra/port.rs | detect_external_metro() at startup + pre-start | WIRED | Lines 557 and 1556 |
| app.rs | ModalState::ExternalMetroConflict | Detection result | WIRED | Line 665 sets modal |
| modals.rs | ModalState::ExternalMetroConflict | Modal rendering | WIRED | Match arm renders PID, directory, Kill/Cancel |
| panels.rs | worktree.stale_pods | Pods icon rendering | WIRED | Line 77 checks wt.stale_pods |
| infra/worktrees.rs | domain/worktree.rs | stale_pods set during enumeration | WIRED | Line 71 calls check_stale_pods, line 82 assigns |
| app.rs | infra/worktrees.rs | All-worktree staleness refresh | WIRED | Line 938 iterates worktrees |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| FIX-01 | 05.2-01, 05.2-05 | Visual overhaul (borders, titles, table, highlight) | SATISFIED | BorderType::Double, title_style(White), 4-column layout, green bold metro row, separate icons |
| FIX-02 | 05.2-02, 05.2-06 | Data dependency model (refresh_needed) | SATISFIED | RefreshSet + refresh_needed() with 20 tests; multi-sentinel staleness detection |
| FIX-03 | 05.2-02, 05.2-06 | Staleness detection rewrite | SATISFIED | check_stale() with .yarn-integrity/.yarn-state; all-worktree refresh post-command |
| FIX-04 | 05.2-04 | External metro conflict detection | SATISFIED | detect_external_metro(), modal, kill, auto-start flow |
| FIX-05 | 05.2-03, 05.2-07 | Universal vim scrolling, auto-follow, scrollbar | SATISFIED | hjkl/G/gg, auto_follow flag, ScrollbarState(max_scroll) |
| FIX-06 | 05.2-01, 05.2-03, 05.2-05, 05.2-07 | Bug fixes (iOS picker, debugger, fullscreen Tab, metro highlight) | SATISFIED | xctrace parsing, 'd' command, Tab exit, separate pods icons |

**Note:** FIX-01 through FIX-06 are defined in ROADMAP.md but not in REQUIREMENTS.md. These are ad-hoc milestone feedback requirements. All are satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | No anti-patterns found in phase-modified files |

### Build Verification

- `cargo check` passes (4 warnings in unrelated `src/infra/multiplexer.rs`)
- `cargo test domain::refresh` passes: 20/20 tests

### Corrections from Previous Verification

The previous verification (2026-03-09) contained two inaccuracies in its evidence descriptions:
- Truth 7 claimed `fg(Color::White).bg(Color::DarkGray)` but actual code is `fg(Color::Green).add_modifier(Modifier::BOLD)` -- the DarkGray bg is the table selection cursor, not the metro-active indicator
- Truth 18 claimed debugger sends `b"j\n"` but actual code sends `b"d\n"` (the React Native CLI debugger toggle key)
- Truth 6 claimed `Length(4)` but actual code is `Length(6)` for the status icon column

These were description errors only; the underlying features work correctly.

### Human Verification Required

### 1. Double-line border visual appearance

**Test:** Launch the dashboard and observe all four pane borders
**Expected:** All panes have double-line (Unicode box-drawing) borders with white titles visible on dark terminals
**Why human:** Visual rendering depends on terminal font and Unicode support

### 2. Metro auto-follow behavior

**Test:** Start metro, observe log scrolling, then scroll up with k, then press G
**Expected:** Auto-follow initially tails log. k disables auto-follow. G re-enables it. Scrollbar position matches content.
**Why human:** Requires running metro with live log output

### 3. iOS physical device picker

**Test:** Press i>d with a physical iOS device connected
**Expected:** Shows connected physical device(s), not simulator list
**Why human:** Requires physical iOS device and Xcode toolchain

### 4. External metro conflict modal

**Test:** Start metro externally, then try starting metro from dashboard
**Expected:** Modal shows PID and working directory with Kill/Cancel options
**Why human:** Requires external process on port 8081

### 5. Staleness indicators accuracy

**Test:** Check worktree table after yarn install in a stale worktree
**Expected:** Stale indicator clears; separate yarn/pods icons shown correctly
**Why human:** Requires real worktrees with varying staleness states

---

_Verified: 2026-03-10T10:00:00Z_
_Verifier: Claude (gsd-verifier)_
