---
phase: 05.2-milestone-feedbacks
plan: 10
subsystem: ui
tags: [ratatui, domain, worktree, naming]

# Dependency graph
requires:
  - phase: 04-config-and-jira-integration
    provides: "JIRA key extraction and title caching"
  - phase: 05-worktree-switching-and-claude-code
    provides: "OpenClaudeCode handler and multiplexer integration"
provides:
  - "preferred_prefix() single source of truth for worktree display name"
  - "jira_key field on Worktree struct"
affects: [ui, worktree-display]

# Tech tracking
tech-stack:
  added: []
  patterns: ["preferred_prefix() as single naming source of truth"]

key-files:
  created: []
  modified:
    - src/domain/worktree.rs
    - src/infra/worktrees.rs
    - src/app.rs
    - src/ui/panels.rs

key-decisions:
  - "jira_key stored on Worktree struct to avoid domain calling infra"
  - "preferred_prefix() returns owned String for flexibility across call sites"
  - "Metro pane title looks up worktree by path match to resolve preferred_prefix"

patterns-established:
  - "preferred_prefix(): label > jira_key > branch > dir basename for all naming contexts"

requirements-completed: [FIX-01]

# Metrics
duration: 2min
completed: 2026-03-11
---

# Phase 05.2 Plan 10: Preferred Prefix Summary

**Single domain function preferred_prefix() for consistent worktree naming in Claude tabs and metro pane title**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-11T10:46:11Z
- **Completed:** 2026-03-11T10:48:08Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Created preferred_prefix() domain function with priority: label > jira_key > branch > dir basename
- Added jira_key field to Worktree struct, populated during worktree parsing
- OpenClaudeCode tab name uses preferred_prefix() instead of directory basename
- Metro pane title resolves worktree and shows preferred_prefix() instead of raw directory name

## Task Commits

Each task was committed atomically:

1. **Task 1: Create preferred_prefix() and wire into OpenClaudeCode** - `5e238ce` (feat)
2. **Task 2: Wire preferred_prefix into metro pane title** - `3e8d995` (feat)

## Files Created/Modified
- `src/domain/worktree.rs` - Added jira_key field and preferred_prefix() method
- `src/infra/worktrees.rs` - Populate jira_key during worktree parsing
- `src/app.rs` - OpenClaudeCode uses preferred_prefix() for tab name
- `src/ui/panels.rs` - Metro pane title resolves preferred_prefix() via worktree lookup

## Decisions Made
- Added jira_key: Option<String> to Worktree struct rather than calling infra::jira from domain -- keeps domain layer pure per ARCH-01
- preferred_prefix() returns owned String (not &str) since jira_key clone and path fallback require allocation
- Metro pane title matches worktree by comparing path basename to worktree_id, falling back to raw worktree_id if no match found

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- preferred_prefix() available for any future naming context
- display_name() preserved (with #[allow(dead_code)]) for longer-form display contexts

---
*Phase: 05.2-milestone-feedbacks*
*Completed: 2026-03-11*
