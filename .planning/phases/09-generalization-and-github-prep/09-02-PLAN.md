---
phase: 09-generalization-and-github-prep
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - LICENSE
  - README.md
  - Cargo.toml
  - .gitignore
autonomous: true
requirements:
  - GH-01
  - GH-02
  - GH-03
  - GH-04

must_haves:
  truths:
    - "MIT license file exists in repo root with correct year and author"
    - "Cargo.toml has description, license, repository, homepage, and keywords"
    - "README covers project description, build instructions, usage guide, and config reference"
    - ".gitignore excludes .planning/, credentials, and build artifacts"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "README.md"
      provides: "Project documentation for GitHub"
      contains: "rn-dash"
    - path: "Cargo.toml"
      provides: "Package metadata for crates.io and GitHub"
      contains: "license"
    - path: ".gitignore"
      provides: "Comprehensive ignore rules"
      contains: ".planning"
  key_links:
    - from: "README.md"
      to: "config.example.json"
      via: "Config reference section describes fields from example file"
      pattern: "config.example.json"
---

<objective>
Add MIT license, complete Cargo.toml metadata, audit .gitignore, and write a comprehensive README with build instructions, usage guide, and config reference.

Purpose: GH-01 through GH-04 prepare the repo for public GitHub release.
Output: LICENSE, updated Cargo.toml, updated .gitignore, README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-generalization-and-github-prep/09-01-SUMMARY.md
@Cargo.toml
@.gitignore
@config.example.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LICENSE, update Cargo.toml metadata, audit .gitignore</name>
  <files>LICENSE, Cargo.toml, .gitignore</files>
  <read_first>
    - Cargo.toml (full file -- see current [package] section)
    - .gitignore (full file -- currently only has /target and .claude/worktrees/)
  </read_first>
  <action>
    **1. Create LICENSE file (repo root):**
    Standard MIT license text. Year: 2026. Copyright holder: "rn-dash contributors".

    **2. Update Cargo.toml [package] section:**
    Keep existing fields (name should now be "rn-dash" from Plan 01). Add after `edition`:
    ```toml
    description = "Terminal dashboard for managing React Native worktrees, metro, and git"
    license = "MIT"
    repository = "https://github.com/AliMonemian/rn-dash"
    homepage = "https://github.com/AliMonemian/rn-dash"
    keywords = ["ratatui", "react-native", "terminal", "dashboard", "worktree"]
    categories = ["command-line-utilities", "development-tools"]
    ```
    Note: The repository URL uses AliMonemian based on the git user "Ali Monemian". If the actual GitHub org differs, the executor should check `git remote -v` and use the real origin URL.

    **3. Audit and update .gitignore:**
    The current .gitignore only has `/target` and `.claude/worktrees/`. Add:
    ```
    # Build artifacts
    /target

    # Planning docs (not shipped)
    .planning/

    # Claude Code worktrees
    .claude/worktrees/

    # Credentials and config (never commit real config with tokens)
    config.json

    # OS files
    .DS_Store
    Thumbs.db

    # Editor files
    *.swp
    *.swo
    *~
    .idea/
    .vscode/
    ```
    Keep `/target` and `.claude/worktrees/` from existing file. Do NOT add `config.example.json` to .gitignore (it should be committed).
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && test -f LICENSE && grep "MIT" LICENSE && grep 'license = "MIT"' Cargo.toml && grep '.planning' .gitignore && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - test -f LICENSE succeeds (file exists)
    - grep "MIT License" LICENSE returns a match
    - grep 'description' Cargo.toml returns a match
    - grep 'license = "MIT"' Cargo.toml returns a match
    - grep 'repository' Cargo.toml returns a match
    - grep 'keywords' Cargo.toml returns a match
    - grep '.planning' .gitignore returns a match
    - grep 'DS_Store' .gitignore returns a match
    - grep 'config.json' .gitignore returns a match (credentials protection)
  </acceptance_criteria>
  <done>MIT LICENSE file present. Cargo.toml has full metadata. .gitignore covers .planning/, credentials, OS files, editor files, build artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive README.md</name>
  <files>README.md</files>
  <read_first>
    - config.example.json (to reference all config fields accurately)
    - Cargo.toml (to get accurate package name and description)
    - src/infra/config.rs (to confirm DashConfig field names and defaults)
    - .planning/phases/09-generalization-and-github-prep/09-01-SUMMARY.md (to see what config fields were created)
  </read_first>
  <action>
    Create `README.md` in repo root with these sections:

    **1. Header:**
    ```markdown
    # rn-dash

    Terminal dashboard for managing React Native worktrees, metro, and git.

    Built with [Ratatui](https://ratatui.rs) in Rust.
    ```

    **2. Features (bulleted list):**
    - Browse and switch between git worktrees
    - Start/stop/reload Metro bundler with one key
    - Run React Native commands (iOS/Android) with device picker
    - Yarn and pod-install via command palette
    - JIRA ticket title integration (auto-fetches from branch names)
    - Open Claude Code in tmux/zellij splits
    - Context-sensitive keybindings with dynamic hints

    **3. Screenshot:**
    ```markdown
    <!-- TODO: Add screenshot -->
    ```
    (User will add later per CONTEXT.md decision)

    **4. Installation / Build:**
    Prerequisites: Rust toolchain (rustup.rs)
    ```bash
    git clone https://github.com/AliMonemian/rn-dash.git
    cd rn-dash
    cargo build --release
    # Binary at target/release/rn-dash
    ```

    **5. Configuration:**
    - Config file location: `~/.config/rn-dash/config.json`
    - Copy the example: `cp config.example.json ~/.config/rn-dash/config.json`
    - Edit with your values
    - Reference table with all fields, their types, defaults, and descriptions. Read actual DashConfig fields from config.rs to build this table. Include:
      | Field | Type | Default | Description |
      with rows for: repo_root (required), jira_base_url, jira_email, jira_token, auth_mode (default: cloud), jira_project_prefix (default: UMP), app_title (default: RN Dash), claude_flags (default: --dangerously-skip-permissions)

    **6. Usage:**
    - Launch: `rn-dash` (or `cargo run` from source)
    - Basic keybindings section (concise table):
      | Key | Action |
      | --- | --- |
      | j/k or arrows | Navigate worktree list |
      | Enter | Start metro / Run on device |
      | Esc | Stop metro |
      | y | Open yarn palette |
      | w | Open worktree palette |
      | c | Open Claude Code |
      | R | Reload metro (when running) |
      | ? | Toggle help overlay |
      | q | Quit |

    **7. License:**
    ```markdown
    [MIT](LICENSE)
    ```

    Keep the README concise -- no more than ~120 lines. No emojis.
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && test -f README.md && grep "rn-dash" README.md && grep "config.example.json" README.md && grep "cargo build" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - test -f README.md succeeds
    - grep "rn-dash" README.md returns a match (project name)
    - grep "cargo build --release" README.md returns a match (build instructions)
    - grep "config.example.json" README.md returns a match (config reference)
    - grep "repo_root" README.md returns a match (config field documented)
    - grep "jira_project_prefix" README.md returns a match (config field documented)
    - grep "MIT" README.md returns a match (license reference)
    - grep "Keybinding\|Key.*Action\|key.*action" README.md returns a match (usage section)
  </acceptance_criteria>
  <done>README.md covers project description, features, build instructions, config reference table with all fields, usage guide with keybinding table, and license link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates static documentation files only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | I (Info Disclosure) | .gitignore | mitigate | Audit ensures .planning/, config.json (credentials), and OS metadata files are excluded from git |
</threat_model>

<verification>
- `test -f LICENSE` passes
- `test -f README.md` passes
- `cargo build` still succeeds (Cargo.toml changes valid)
- `grep '.planning' .gitignore` returns match
- README has sections: Features, Build, Configuration, Usage, License
</verification>

<success_criteria>
Repo has MIT license, complete Cargo.toml metadata, comprehensive .gitignore, and a README that a new user can follow to build, configure, and use the tool.
</success_criteria>

<output>
After completion, create `.planning/phases/09-generalization-and-github-prep/09-02-SUMMARY.md`
</output>
