---
phase: 05.1-milestone-feedback
plan: "06"
subsystem: ui
tags: [ratatui, modal, command-queue, clean, sync, shell]

# Dependency graph
requires:
  - phase: 05.1-01
    provides: command_queue VecDeque<CommandSpec> and dispatch_command() helper
  - phase: 05.1-03
    provides: CleanOptions, ModalState::CleanToggle, ModalState::SyncBeforeRun, CommandSpec::ShellCommand domain types
  - phase: 05.1-05
    provides: handle_key() keybindings for clean toggle and sync-before-run modals
provides:
  - CleanToggle modal fully wired: toggles mutate CleanOptions, CleanConfirm enqueues command sequence
  - SyncBeforeRun modal replaces WORK-06 lazy install: prompts user on stale worktree before run command
  - ShellCommand via StartShellCommand opens TextInput modal, ModalInputSubmit handles ShellCommand template
  - render_clean_modal() with n/p/a/i checkboxes and Cyan border
  - render_sync_prompt() with stale warning and Yellow border
  - SimulatorUsed action records to sim_history via spawn_blocking
affects:
  - 05.1-07 (sim history sort will use SimulatorUsed events)
  - 06 (future phases using command queue)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "modal-first dispatch: CleanConfirm takes modal.take(), builds Vec<CommandSpec>, dispatches first, queues rest"
    - "sync-before-run gate: CommandRun intercepts stale+run and shows SyncBeforeRun modal before queue entry"

key-files:
  created: []
  modified:
    - src/app.rs
    - src/ui/modals.rs

key-decisions:
  - "SyncBeforeRun replaces WORK-06 lazy install — user-visible prompt is better UX than silent yarn install"
  - "CleanConfirm builds all commands first, removes first from Vec, dispatches it, pushes rest to queue — avoids double-first-dispatch bug"
  - "render_placeholder_modal removed — all ModalState variants now have real renderers"

patterns-established:
  - "Modal actions pattern: take() modal, match inner data, act on data — CleanConfirm, SyncBeforeRunAccept/Decline all follow this"

requirements-completed:
  - PHASE-05.1-CLEAN-MODAL
  - PHASE-05.1-SYNC-PROMPT
  - PHASE-05.1-SHELL-CMD

# Metrics
duration: 2min
completed: 2026-03-03
---

# Phase 05.1 Plan 06: Modal Workflows (Clean, Sync, Shell) Summary

**CleanToggle and SyncBeforeRun modals fully wired into command queue with rendered UI — replacing WORK-06 lazy install with user-visible sync prompt**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-03T04:33:51Z
- **Completed:** 2026-03-03T04:35:25Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- CleanToggle modal actions (n/p/a/i toggles, x/Enter confirm) mutate CleanOptions and enqueue command sequence
- CommandRun now shows SyncBeforeRun modal prompt instead of silently running yarn install for stale worktrees
- StartShellCommand opens TextInput modal; ModalInputSubmit handles ShellCommand template arm
- render_clean_modal() renders checkboxes with current toggle state (Cyan border)
- render_sync_prompt() renders stale warning with sync description and run command label (Yellow border)

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire clean toggle, sync-before-run, and shell command actions in update()** - `053615f` (feat)
2. **Task 2: Render CleanToggle and SyncBeforeRun modals** - `8f92f5c` (feat)

**Plan metadata:** (final commit after summary)

## Files Created/Modified
- `src/app.rs` - CleanToggle toggle actions, CleanConfirm queue builder, SyncBeforeRun gate in CommandRun, SyncBeforeRunAccept/Decline handlers, StartShellCommand, SimulatorUsed, ShellCommand arm in ModalInputSubmit
- `src/ui/modals.rs` - render_modal() dispatches CleanToggle/SyncBeforeRun, added render_clean_modal(), render_sync_prompt(), removed render_placeholder_modal()

## Decisions Made
- SyncBeforeRun replaces WORK-06 lazy install — user-visible prompt gives user agency over whether to sync before running
- CleanConfirm builds all commands first, removes first, dispatches it, pushes rest to queue — avoids double-dispatch
- render_placeholder_modal removed since all ModalState variants now have real renderers

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All three modal workflows (clean, sync, shell) are fully functional with command queue integration
- Plan 07 (sim history sort) can use SimulatorUsed events now being recorded
- `cargo build` succeeds with only pre-existing warnings (not introduced by this plan)

---
*Phase: 05.1-milestone-feedback*
*Completed: 2026-03-03*
