---
phase: quick-260331-cw5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infra/devices.rs
  - src/domain/command.rs
  - src/infra/android_prefs.rs
  - src/infra/mod.rs
  - src/app.rs
  - src/ui/footer.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Android device picker shows model names instead of serial numbers"
    - "run-android command includes --mode debugOptimized (or saved mode)"
    - "Last-used Android mode is persisted and auto-applied on next run"
  artifacts:
    - path: "src/infra/devices.rs"
      provides: "Updated adb parser using -l flag for model names"
    - path: "src/infra/android_prefs.rs"
      provides: "Android mode persistence (load/save)"
    - path: "src/domain/command.rs"
      provides: "RnRunAndroid with optional mode field"
  key_links:
    - from: "src/app.rs"
      to: "src/infra/android_prefs.rs"
      via: "load prefs on init, save on android run"
      pattern: "android_prefs"
    - from: "src/domain/command.rs"
      to: "to_argv()"
      via: "mode inserted into argv when Some"
      pattern: "--mode"
---

<objective>
Fix Android device listing to show model names (using `adb devices -l`), add `--mode debugOptimized` to run-android, and persist the last-used mode for auto-application.

Purpose: Better device identification in picker + faster debug builds with debugOptimized mode.
Output: Updated device parser, mode-aware run command, persistent mode preference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/infra/devices.rs
@src/domain/command.rs
@src/infra/sim_history.rs
@src/app.rs
@src/ui/footer.rs
@src/infra/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix adb parser for model names and add android_prefs persistence</name>
  <files>src/infra/devices.rs, src/infra/android_prefs.rs, src/infra/mod.rs</files>
  <action>
1. **src/infra/devices.rs** — Update `parse_adb_devices()` to parse `adb devices -l` output format:
   - Sample line: `emulator-5552  device product:sdk_gphone64_arm64 model:sdk_gphone64_arm64 device:emu64a transport_id:3`
   - Parse model from `model:XXX` field. Replace underscores with spaces for display name.
   - If no model field found, fall back to serial as name (current behavior).
   - Update `list_android_devices()` to pass `-l` flag: `.args(["devices", "-l"])` instead of `.arg("devices")`.
   - Update the doc comment and sample output to reflect `-l` format.

2. **src/infra/android_prefs.rs** — New file, follow `sim_history.rs` pattern:
   - `android_prefs_path() -> PathBuf` using `config_dir().join("android_prefs.json")`
   - `load_android_mode() -> Option<String>` — reads JSON `{"mode": "debugOptimized"}`, returns the mode string or None.
   - `save_android_mode(mode: &str) -> anyhow::Result<()>` — writes the JSON file, creates config dir if needed.
   - Use `serde_json::json!` macro for simple serialization, same as sim_history pattern.

3. **src/infra/mod.rs** — Add `pub mod android_prefs;` line.
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>parse_adb_devices handles -l format with model names; android_prefs module exists with load/save functions; cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire mode into RnRunAndroid and app logic</name>
  <files>src/domain/command.rs, src/app.rs, src/ui/footer.rs</files>
  <action>
1. **src/domain/command.rs** — Add `mode` field to `RnRunAndroid`:
   - Change `RnRunAndroid { device_id: String }` to `RnRunAndroid { device_id: String, mode: Option<String> }`.
   - Update `to_argv()`: if mode is Some, insert `"--mode".into(), mode.clone()` before `"--deviceId"` in the argv vec.
   - Update all pattern matches on `RnRunAndroid` throughout the file (label, needs_device_selection, etc.) to use `..` for the new field where not needed.

2. **src/app.rs** — Wire mode through all RnRunAndroid dispatch points:
   - Add `android_mode: Option<String>` field to `AppState`, initialized from `android_prefs::load_android_mode()` during app init.
   - In the Android palette handler (around line 285-286): pass `mode: state.android_mode.clone()` when constructing `RnRunAndroid`.
   - In `ModalDeviceConfirm` handler (around line 1176): when constructing the real `RnRunAndroid` from device picker, copy mode from the pending_template.
   - In `DevicesEnumerated` single-device auto-select (around line 1207): same — copy mode from the spec.
   - After `dispatch_command` for any `RnRunAndroid`, call `save_android_mode` if mode is Some:
     ```rust
     if let CommandSpec::RnRunAndroid { mode: Some(ref m), .. } = real_spec {
         let _ = crate::infra::android_prefs::save_android_mode(m);
     }
     ```
   - The mode is always auto-applied from saved prefs. No interactive mode selection needed.

3. **src/ui/footer.rs** — Update Android palette hint for 'd' from "run-android" to "run-android (debug)" to indicate mode awareness. No other footer changes needed.
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>RnRunAndroid carries mode field; all construction sites pass mode from AppState; mode persisted on dispatch; cargo check passes with no errors.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/cubicme/aljazeera/dashboard && cargo check 2>&1 | tail -5
```
No compiler errors. All `RnRunAndroid` pattern matches updated for the new `mode` field.
</verification>

<success_criteria>
- `adb devices -l` is used instead of `adb devices`
- Model names parsed and displayed in device picker
- `--mode debugOptimized` (or saved mode) appears in run-android argv
- Mode persisted to ~/.config/ump-dash/android_prefs.json
- Mode auto-loaded on app start and applied to all subsequent android runs
- cargo check passes cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260331-cw5-fix-android-device-listing-add-mode-debu/260331-cw5-SUMMARY.md`
</output>
