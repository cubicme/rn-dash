---
phase: 09-generalization-and-github-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/infra/config.rs
  - src/infra/jira.rs
  - src/app.rs
  - src/ui/panels.rs
  - src/tui.rs
  - src/main.rs
  - config.example.json
autonomous: true
requirements:
  - GEN-01
  - GEN-02

must_haves:
  truths:
    - "App reads repo_root from config file instead of hardcoding ~/aljazeera/ump"
    - "JIRA ticket extraction uses configurable project prefix instead of hardcoded UMP"
    - "Config directory is ~/.config/rn-dash/ (not ump-dash)"
    - "Title bar shows RN Dash (not UMP Dashboard)"
    - "Config example file documents all settings with explanatory comments"
    - "App compiles and runs with zero warnings"
  artifacts:
    - path: "src/infra/config.rs"
      provides: "DashConfig with repo_root, jira_project_prefix, app_name fields; config_dir returns rn-dash"
      contains: "rn-dash"
    - path: "src/infra/jira.rs"
      provides: "extract_jira_key accepts project prefix parameter"
      contains: "fn extract_jira_key"
    - path: "config.example.json"
      provides: "Documented example config for new users"
      contains: "repo_root"
  key_links:
    - from: "src/app.rs"
      to: "src/infra/config.rs"
      via: "DashConfig.repo_root used in AppState default and run()"
      pattern: "repo_root"
    - from: "src/app.rs"
      to: "src/infra/jira.rs"
      via: "extract_jira_key called with prefix from config"
      pattern: "extract_jira_key"
---

<objective>
Extract all hardcoded AJ/UMP/system-specific values from the codebase into DashConfig fields so the app works for any React Native monorepo without source changes.

Purpose: GEN-01 requires no hardcoded org-specific values; GEN-02 requires a documented config example file.
Output: Updated config.rs with new fields, all call sites wired, config.example.json in repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/infra/config.rs
@src/infra/jira.rs
@src/app.rs
@src/ui/panels.rs
@src/tui.rs
@src/main.rs
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize config struct, config_dir, JIRA extraction, and all hardcoded references</name>
  <files>Cargo.toml, src/infra/config.rs, src/infra/jira.rs, src/app.rs, src/ui/panels.rs, src/tui.rs, src/main.rs</files>
  <read_first>
    - src/infra/config.rs (full file -- DashConfig struct, config_dir, load_config, save_config)
    - src/infra/jira.rs (full file -- extract_jira_key function and its tests)
    - src/app.rs (lines 63-210 for AppState struct and Default impl; lines 1920-1960 for config loading in run())
    - src/ui/panels.rs (lines 18-26 for render_title_bar with hardcoded "UMP Dashboard")
    - src/tui.rs (full file -- log dir and log filename using "ump-dash")
    - src/main.rs (full file -- tracing messages saying "ump-dash")
    - Cargo.toml (full file -- package name is "ump-dash")
  </read_first>
  <action>
    **1. Rename package (Cargo.toml):**
    - Change `name = "ump-dash"` to `name = "rn-dash"`

    **2. Update config_dir (src/infra/config.rs):**
    - Change `config_dir()` to return `~/.config/rn-dash/` instead of `~/.config/ump-dash/`

    **3. Add new fields to DashConfig (src/infra/config.rs):**
    Add these fields with serde defaults so existing config files still load:
    - `repo_root: Option<String>` -- serde default None. If None, app should require it (no silent fallback to a hardcoded path)
    - `jira_project_prefix: String` -- serde default `"UMP"` via `fn default_jira_prefix() -> String { "UMP".to_string() }`
    - `app_title: String` -- serde default `"RN Dash"` via `fn default_app_title() -> String { "RN Dash".to_string() }`

    Add a helper method on DashConfig:
    ```rust
    pub fn repo_root_path(&self) -> Option<std::path::PathBuf> {
        self.repo_root.as_ref().map(|s| {
            if s.starts_with("~/") {
                let home = std::env::var("HOME").unwrap_or_else(|_| ".".into());
                std::path::PathBuf::from(home).join(&s[2..])
            } else {
                std::path::PathBuf::from(s)
            }
        })
    }
    ```

    **4. Update extract_jira_key (src/infra/jira.rs):**
    - Change signature from `pub fn extract_jira_key(branch: &str) -> Option<String>` to `pub fn extract_jira_key(branch: &str, project_prefix: &str) -> Option<String>`
    - Replace hardcoded `"UMP"` comparison with `project_prefix` parameter
    - Replace `format!("UMP-{}", second)` with `format!("{}-{}", project_prefix, second)`
    - Update all tests to pass `"UMP"` as the second argument (behavior unchanged for existing tests)
    - Add one test with a different prefix (e.g., `extract_jira_key("feature/PROJ-42-thing", "PROJ")` returns `Some("PROJ-42")`)

    **5. Wire repo_root from config (src/app.rs):**
    - In the `run()` function (around line 1927), after loading config, set `state.repo_root` from config:
      ```rust
      if let Some(path) = config.repo_root_path() {
          state.repo_root = path;
      }
      ```
    - Remove the hardcoded `$HOME/aljazeera/ump` default from `AppState::default()`. Instead set `repo_root: PathBuf::new()` (will be overwritten by config in run()).
    - Store `jira_project_prefix` on AppState: add field `pub jira_project_prefix: String` with default `"UMP".to_string()`. In run(), after loading config: `state.jira_project_prefix = config.jira_project_prefix.clone();`

    **6. Update all extract_jira_key call sites (src/app.rs or wherever called):**
    - grep for `extract_jira_key(` and pass `&state.jira_project_prefix` (or the appropriate variable in scope) as the second argument
    - If called in domain/worktree.rs, that code may need to accept a prefix parameter. Check `src/domain/worktree.rs` for calls.

    **7. Update title bar (src/ui/panels.rs):**
    - Change `.title(" UMP Dashboard ")` to `.title(format!(" {} ", state.config.as_ref().map(|c| c.app_title.as_str()).unwrap_or("RN Dash")))`
    - The `_state` parameter is already passed as `state: &AppState` -- just remove the underscore prefix

    **8. Update log paths (src/tui.rs):**
    - Change `"ump-dash"` in the log directory path to `"rn-dash"`
    - Change `"ump-dash.log"` log filename to `"rn-dash.log"`

    **9. Update tracing messages (src/main.rs):**
    - Change `"ump-dash starting"` to `"rn-dash starting"`
    - Change `"ump-dash exiting"` to `"rn-dash exiting"`

    **10. Update comments throughout:**
    - Fix doc comments in config.rs, jira.rs, jira_cache.rs, android_prefs.rs, sim_history.rs that reference "ump-dash" to say "rn-dash"
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && cargo build 2>&1 | tail -5 && cargo test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "ump-dash" src/ returns zero matches (all renamed to rn-dash)
    - grep -r "aljazeera/ump" src/ returns zero matches (no hardcoded repo path)
    - grep 'fn extract_jira_key' src/infra/jira.rs shows project_prefix parameter
    - grep 'repo_root' src/infra/config.rs shows the field exists in DashConfig
    - grep 'jira_project_prefix' src/infra/config.rs shows the field exists in DashConfig
    - cargo build succeeds with no errors
    - cargo test succeeds (all existing + new prefix test pass)
  </acceptance_criteria>
  <done>All hardcoded AJ/UMP/system-specific values extracted to DashConfig fields. Package renamed to rn-dash. Config dir is ~/.config/rn-dash/. extract_jira_key accepts configurable prefix. Title bar reads from config. Compiles and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create config.example.json with documented settings</name>
  <files>config.example.json</files>
  <read_first>
    - src/infra/config.rs (to see final DashConfig fields after Task 1)
  </read_first>
  <action>
    Create `config.example.json` in the repo root. Since JSON does not support comments natively, use a companion approach: create the file as valid JSON with descriptive field names, and add a `"_comment_*"` field before each real field to serve as documentation. Structure:

    ```json
    {
      "_comment_repo_root": "Absolute path to your React Native monorepo root (supports ~/). Required.",
      "repo_root": "~/code/my-rn-app",

      "_comment_jira_base_url": "Base URL for your JIRA instance. Required for JIRA integration.",
      "jira_base_url": "https://your-org.atlassian.net",

      "_comment_jira_email": "JIRA account email. Required for Cloud auth mode, unused for Data Center.",
      "jira_email": "you@example.com",

      "_comment_jira_token": "JIRA API token (Cloud) or Personal Access Token (Data Center).",
      "jira_token": "your-api-token-here",

      "_comment_auth_mode": "Authentication mode: 'cloud' (Basic Auth) or 'datacenter' (Bearer PAT). Default: cloud.",
      "auth_mode": "cloud",

      "_comment_jira_project_prefix": "JIRA project key prefix used in branch names (e.g., PROJ for PROJ-1234). Default: UMP.",
      "jira_project_prefix": "PROJ",

      "_comment_app_title": "Title shown in the dashboard header. Default: RN Dash.",
      "app_title": "RN Dash",

      "_comment_claude_flags": "Flags passed when launching Claude Code. Default: --dangerously-skip-permissions.",
      "claude_flags": "--dangerously-skip-permissions"
    }
    ```

    Ensure the file is valid JSON (no trailing commas). Use placeholder values that make it clear the user needs to replace them.
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && python3 -c "import json; json.load(open('config.example.json')); print('valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - config.example.json exists in repo root
    - python3 -c "import json; json.load(open('config.example.json'))" succeeds (valid JSON)
    - grep 'repo_root' config.example.json returns a match
    - grep 'jira_project_prefix' config.example.json returns a match
    - grep 'jira_base_url' config.example.json returns a match
    - All DashConfig fields are represented in the example file
  </acceptance_criteria>
  <done>config.example.json exists in repo root with all DashConfig fields documented via _comment_ pattern, placeholder values, and valid JSON syntax.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> app | User-provided config values (paths, URLs) are read at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T (Tampering) | config.rs | accept | Config file is already chmod 600; user controls their own config |
| T-09-02 | I (Info Disclosure) | config.example.json | mitigate | Example file uses placeholder values only, never real credentials; .gitignore already excludes config dir |
</threat_model>

<verification>
- `cargo build` succeeds
- `cargo test` succeeds
- `grep -r "ump-dash" src/` returns 0 matches
- `grep -r "aljazeera" src/` returns 0 matches
- `config.example.json` is valid JSON
</verification>

<success_criteria>
No hardcoded AJ/UMP/system-specific values remain in source. All configurable via DashConfig. Config example file documents every field.
</success_criteria>

<output>
After completion, create `.planning/phases/09-generalization-and-github-prep/09-01-SUMMARY.md`
</output>
