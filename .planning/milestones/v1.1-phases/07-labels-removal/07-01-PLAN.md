---
phase: 07-labels-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infra/labels.rs
  - src/infra/mod.rs
  - src/infra/config.rs
  - src/infra/jira_cache.rs
  - src/infra/android_prefs.rs
  - src/infra/sim_history.rs
  - src/infra/worktrees.rs
  - src/domain/worktree.rs
  - src/action.rs
  - src/app.rs
  - src/ui/panels.rs
  - src/ui/footer.rs
  - src/ui/help_overlay.rs
autonomous: true
requirements: [CLN-01]

must_haves:
  truths:
    - "No label-related UI elements or keybindings exist in the dashboard"
    - "No label files are written or read on startup"
    - "Codebase compiles with zero warnings related to labels"
    - "Worktree list renders correctly with branch and JIRA title only (no label column)"
  artifacts:
    - path: "src/infra/config.rs"
      provides: "config_dir() function relocated from labels.rs"
      contains: "pub fn config_dir"
    - path: "src/domain/worktree.rs"
      provides: "Worktree struct without label field"
    - path: "src/action.rs"
      provides: "Action enum without SetLabel/StartSetLabel variants"
  key_links:
    - from: "src/infra/jira_cache.rs"
      to: "src/infra/config.rs"
      via: "config_dir import"
      pattern: "use crate::infra::config::config_dir"
    - from: "src/infra/android_prefs.rs"
      to: "src/infra/config.rs"
      via: "config_dir import"
      pattern: "use crate::infra::config::config_dir"
    - from: "src/infra/sim_history.rs"
      to: "src/infra/config.rs"
      via: "config_dir import"
      pattern: "use crate::infra::config::config_dir"
---

<objective>
Remove the labels feature entirely from the codebase: domain types, infra persistence, action variants, app state/handlers, UI rendering, keybindings, and help text.

Purpose: Eliminate dead feature code before Phase 08 keybinding rework, simplifying the codebase.
Output: Clean compilation with no label references; `src/infra/labels.rs` deleted; `config_dir()` relocated to `config.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/infra/labels.rs
@src/infra/config.rs
@src/infra/mod.rs
@src/domain/worktree.rs
@src/action.rs
@src/app.rs
@src/ui/panels.rs
@src/ui/footer.rs
@src/ui/help_overlay.rs

<interfaces>
<!-- config_dir() is currently in labels.rs and imported by 4 other infra modules -->

From src/infra/labels.rs (to be relocated to config.rs):
```rust
pub fn config_dir() -> std::path::PathBuf {
    let home = std::env::var("HOME").unwrap_or_else(|_| "~".to_string());
    std::path::PathBuf::from(home).join(".config").join("ump-dash")
}
```

Current consumers of config_dir via labels.rs:
- src/infra/config.rs: `use crate::infra::labels::config_dir;`
- src/infra/jira_cache.rs: `use crate::infra::labels::config_dir;`
- src/infra/android_prefs.rs: `use crate::infra::labels::config_dir;`
- src/infra/sim_history.rs: `use crate::infra::labels::config_dir;`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate config_dir and remove labels infra + domain + action</name>
  <files>src/infra/config.rs, src/infra/labels.rs, src/infra/mod.rs, src/infra/jira_cache.rs, src/infra/android_prefs.rs, src/infra/sim_history.rs, src/infra/worktrees.rs, src/domain/worktree.rs, src/action.rs</files>
  <read_first>
    src/infra/labels.rs
    src/infra/config.rs
    src/infra/mod.rs
    src/infra/jira_cache.rs
    src/infra/android_prefs.rs
    src/infra/sim_history.rs
    src/infra/worktrees.rs
    src/domain/worktree.rs
    src/action.rs
  </read_first>
  <action>
1. **Relocate config_dir() to config.rs:**
   - In `src/infra/config.rs`: Add the `config_dir()` function (copy from labels.rs lines 16-19). Place it near the top, after imports. It returns `PathBuf::from(home).join(".config").join("ump-dash")`. Remove the existing `use crate::infra::labels::config_dir;` import since the function is now local. Remove the comment on lines 7-8 about "re-exported from labels.rs".

2. **Update config_dir consumers:**
   - In `src/infra/jira_cache.rs`: Change `use crate::infra::labels::config_dir;` to `use crate::infra::config::config_dir;`
   - In `src/infra/android_prefs.rs`: Change `use crate::infra::labels::config_dir;` to `use crate::infra::config::config_dir;`
   - In `src/infra/sim_history.rs`: Change `use crate::infra::labels::config_dir;` to `use crate::infra::config::config_dir;`

3. **Delete labels.rs:**
   - Delete the file `src/infra/labels.rs` entirely.
   - In `src/infra/mod.rs`: Remove the line `pub mod labels;`

4. **Clean domain/worktree.rs:**
   - Remove the `pub label: Option<String>` field (line 37) and its doc comments (lines 35-36).
   - In `display_name()`: Remove the `if let Some(label) = &self.label { return label.as_str(); }` block (lines 57-59). The method now returns jira_title first, then branch.
   - In `preferred_prefix()`: Remove the `if let Some(label) = &self.label { return label.clone(); }` block (lines 74-75). The method now returns jira_key first, then branch, then dir name.
   - Update the module doc comment on line 20 to remove "label lookups" — change to just "optional JIRA lookups".

5. **Clean infra/worktrees.rs:**
   - Remove `label: None,` from the Worktree constructor (line 82).

6. **Clean action.rs:**
   - Remove the comment `// Phase 3: Label management` (line 63).
   - Remove `SetLabel { branch: String, label: String },` (line 64).
   - Remove `StartSetLabel,` and its comment (line 65).
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && cargo check 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - `src/infra/labels.rs` does not exist
    - `grep -r "pub mod labels" src/infra/mod.rs` returns no matches
    - `grep "config_dir" src/infra/config.rs` shows `pub fn config_dir`
    - `grep "labels::config_dir" src/infra/jira_cache.rs src/infra/android_prefs.rs src/infra/sim_history.rs` returns no matches
    - `grep "config::config_dir" src/infra/jira_cache.rs src/infra/android_prefs.rs src/infra/sim_history.rs` returns 3 matches
    - `grep "pub label" src/domain/worktree.rs` returns no matches
    - `grep "SetLabel\|StartSetLabel" src/action.rs` returns no matches
    - `grep "label: None" src/infra/worktrees.rs` returns no matches
  </acceptance_criteria>
  <done>labels.rs deleted, config_dir relocated to config.rs, all infra consumers updated, domain Worktree has no label field, Action enum has no label variants</done>
</task>

<task type="auto">
  <name>Task 2: Remove label references from app.rs and UI, verify clean build</name>
  <files>src/app.rs, src/ui/panels.rs, src/ui/footer.rs, src/ui/help_overlay.rs</files>
  <read_first>
    src/app.rs
    src/ui/panels.rs
    src/ui/footer.rs
    src/ui/help_overlay.rs
  </read_first>
  <action>
1. **Clean app.rs state fields:**
   - Remove `pub labels: std::collections::HashMap<String, String>,` field and its comment (lines 105-106).
   - Remove `pub pending_label_branch: Option<String>,` field and its comment (lines 117-118).
   - In `Default` impl: Remove `labels: std::collections::HashMap::new(),` (line 179).
   - In `Default` impl: Remove `pending_label_branch: None,` (line 186).

2. **Clean app.rs keybinding:**
   - In the WorktreeTable key handler, remove `Char('L') => return Some(Action::StartSetLabel),` (line 401).

3. **Clean app.rs WorktreesLoaded handler:**
   - Remove the label reload block (lines 711-717):
     ```
     state.labels = crate::infra::labels::load_labels().unwrap_or_default();
     for wt in &mut worktrees {
         wt.label = state.labels.get(&wt.branch).cloned();
     }
     ```

4. **Clean app.rs ModalInputSubmit handler:**
   - Remove the label submit check block (lines 1093-1103):
     ```
     if let Some(branch) = state.pending_label_branch.take() {
         update(state, Action::SetLabel { branch, label: buffer }, metro_tx, handle_tx);
     } else if ...
     ```
   - The `if let Some(branch)` block should be removed, and the `else if state.pending_android_mode` should become a plain `if state.pending_android_mode` (since the label check before it is gone).

5. **Clean app.rs SetLabel/StartSetLabel handlers:**
   - Remove the entire `Action::SetLabel { branch, label }` match arm (lines 1356-1368).
   - Remove the entire `Action::StartSetLabel` match arm (lines 1371-1385 approximately).
   - Remove the comment `// --- Phase 3: Label management ---` (line 1356).

6. **Clean app.rs startup:**
   - Remove `state.labels = crate::infra::labels::load_labels().unwrap_or_default();` (lines 1795-1796).

7. **Clean UI panels.rs:**
   - Remove `let label = wt.label.as_deref().unwrap_or("");` (line 64).
   - Remove `Cell::from(truncate(label, 12)),` from the row cells (line 113).
   - Remove `Constraint::Length(14), // Label` from the table column constraints (line 160).

8. **Clean UI footer.rs:**
   - Remove `("L", "label"),` from the WorktreeTable panel_hints vec (line 154).

9. **Clean UI help_overlay.rs:**
   - Remove `Row::new(vec!["L", "Set custom branch label"]),` (line 39).

10. **Verify clean build:**
    - Run `cargo build` — must compile with zero errors.
    - Run `cargo clippy` — no label-related warnings.
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && cargo build 2>&1 | tail -5 && echo "---CLIPPY---" && cargo clippy 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` succeeds with exit code 0
    - `grep -rn "label" src/app.rs | grep -iv "// \|spec.label()\|command_label\|run_command.label"` returns no label-management matches (note: `spec.label()` and `run_command.label()` are CommandSpec method calls, NOT the labels feature — those must remain)
    - `grep "pending_label_branch" src/app.rs` returns no matches
    - `grep "pub labels:" src/app.rs` returns no matches
    - `grep "load_labels\|save_labels" src/app.rs` returns no matches
    - `grep '"L".*label' src/ui/footer.rs` returns no matches
    - `grep "Set custom branch label" src/ui/help_overlay.rs` returns no matches
    - `grep "Label" src/ui/panels.rs | grep -i "constraint\|truncate"` returns no matches
    - `grep -rn "infra::labels" src/` returns no matches
    - `cargo clippy 2>&1 | grep -c "warning"` shows 0 or only non-label warnings
  </acceptance_criteria>
  <done>All label references removed from app.rs (state, keybinding, handlers, startup), panels.rs (column), footer.rs (hint), help_overlay.rs (help row). Project compiles cleanly with no label dead code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — this is a pure removal/cleanup phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | config_dir relocation | accept | Function is identical; only the module location changes. No behavioral change. |
</threat_model>

<verification>
1. `cargo build` succeeds — no compilation errors
2. `cargo clippy` — no warnings related to dead code or unused imports from label removal
3. `grep -rn "label" src/ | grep -v "spec.label\|command.*label\|run_command.label\|\.label()"` — only CommandSpec.label() method references remain (those are unrelated to the labels feature)
4. `test -f src/infra/labels.rs` — file does not exist
5. `grep -rn "infra::labels" src/` — no references to the deleted module
</verification>

<success_criteria>
- labels.rs is deleted
- config_dir() lives in config.rs and all 4 consumers import from there
- Worktree struct has no label field
- Action enum has no SetLabel/StartSetLabel variants
- app.rs has no labels HashMap, pending_label_branch, label handlers, or label load on startup
- UI has no label column, no L keybinding hint, no label help text
- `cargo build` and `cargo clippy` pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-labels-removal/07-01-SUMMARY.md`
</output>
