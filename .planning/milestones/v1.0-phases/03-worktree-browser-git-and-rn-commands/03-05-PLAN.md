---
phase: 03-worktree-browser-git-and-rn-commands
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/command.rs
autonomous: true
gap_closure: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-05
  - WORK-06
  - GIT-01
  - GIT-02
  - GIT-03
  - GIT-04
  - GIT-05
  - GIT-06
  - RN-01
  - RN-02
  - RN-03
  - RN-04
  - RN-05
  - RN-06
  - RN-07
  - RN-08
  - RN-09
  - RN-10
  - RN-11
  - RN-12

must_haves:
  truths:
    - "RnCleanAndroid produces argv ['npx', 'react-native', 'clean', '--include', 'android']"
    - "RnCleanCocoapods produces argv ['npx', 'react-native', 'clean', '--include', 'cocoapods']"
    - "RnRunAndroid produces argv ['npx', 'react-native', 'run-android', '--deviceId', device_id]"
    - "RnRunIos produces argv ['yarn', 'react-native', 'run-ios', '--udid', device_id]"
    - "YarnUnitTests produces argv ['yarn', 'unit-tests']"
    - "YarnLint produces argv ['yarn', 'lint', '--quiet', '--fix']"
  artifacts:
    - path: "src/domain/command.rs"
      provides: "Corrected to_argv() and label() for 6 CommandSpec variants"
      contains: "react-native.*clean.*--include"
  key_links:
    - from: "src/domain/command.rs"
      to: "src/infra/command_runner.rs"
      via: "build_argv() delegates to to_argv() for all non-GitResetHard variants"
      pattern: "other => other\\.to_argv\\(\\)"
---

<objective>
Fix six CommandSpec variants whose to_argv() output deviates from requirements RN-01, RN-02, RN-06, RN-07, RN-08, and RN-10.

Purpose: The command dispatch mechanism (device selection, streaming, modals, palette routing) is fully functional but these 6 variants would run different tools than users expect. The fix is localized entirely to to_argv() and label() in src/domain/command.rs.
Output: Corrected command.rs with all 17 CommandSpec variants producing requirement-compliant argv.
</objective>

<execution_context>
@/Users/cubicme/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cubicme/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-worktree-browser-git-and-rn-commands/03-VERIFICATION.md
@src/domain/command.rs
@src/infra/command_runner.rs

<interfaces>
<!-- Key context: build_argv() in command_runner.rs already overrides GitResetHard to use origin/{current_branch}.
     All other variants delegate to CommandSpec::to_argv(). Only to_argv() needs fixing. -->

From src/infra/command_runner.rs:
```rust
fn build_argv(spec: &CommandSpec, current_branch: &str) -> Vec<String> {
    match spec {
        CommandSpec::GitResetHard => {
            vec!["git".into(), "reset".into(), "--hard".into(), format!("origin/{current_branch}")]
        }
        other => other.to_argv(),
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CommandSpec to_argv() and label() for 6 requirement-deviating variants</name>
  <files>src/domain/command.rs</files>
  <action>
In src/domain/command.rs, update the to_argv() method for exactly these 6 variants:

1. RnCleanAndroid (RN-01): Change from `["./gradlew", "clean"]` to `["npx", "react-native", "clean", "--include", "android"]`

2. RnCleanCocoapods (RN-02): Change from `["pod", "deintegrate"]` to `["npx", "react-native", "clean", "--include", "cocoapods"]`

3. RnRunAndroid (RN-06): Change from `["yarn", "android", "--deviceId", device_id]` to `["npx", "react-native", "run-android", "--deviceId", device_id]`

4. RnRunIos (RN-07): Change from `["yarn", "ios", "--udid", device_id]` to `["yarn", "react-native", "run-ios", "--udid", device_id]`

5. YarnUnitTests (RN-08): Change from `["yarn", "test"]` to `["yarn", "unit-tests"]`

6. YarnLint (RN-10): Change from `["yarn", "lint"]` to `["yarn", "lint", "--quiet", "--fix"]`

Also update the label() method to reflect the new commands:
- RnCleanAndroid: "Clean Android (react-native clean)" (was "gradlew clean")
- RnCleanCocoapods: "Clean CocoaPods (react-native clean)" (was "pod deintegrate")
- YarnUnitTests: "yarn unit-tests" (was "yarn test")
- YarnLint: "yarn lint --quiet --fix" (was "yarn lint")

Do NOT change any other variants. The remaining 11 variants are already correct.
Do NOT change is_destructive(), needs_text_input(), needs_device_selection(), or any other method.
Do NOT change command_runner.rs — build_argv() already correctly overrides GitResetHard.
  </action>
  <verify>
    <automated>cd /Users/cubicme/aljazeera/dashboard && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 to_argv() variants produce requirement-compliant argv arrays. Labels updated to match. cargo build succeeds with no errors. No other code changes needed.</done>
</task>

</tasks>

<verification>
1. `cargo build` compiles without errors or new warnings
2. Grep src/domain/command.rs for the corrected argv strings:
   - `react-native.*clean.*--include.*android` present
   - `react-native.*clean.*--include.*cocoapods` present
   - `react-native.*run-android.*--deviceId` present
   - `react-native.*run-ios.*--udid` present
   - `yarn.*unit-tests` present (not `yarn.*test` alone)
   - `yarn.*lint.*--quiet.*--fix` present
3. No other files modified
</verification>

<success_criteria>
All 17 CommandSpec variants produce argv matching their respective requirements (RN-01 through RN-12, GIT-01 through GIT-06). The gap identified in 03-VERIFICATION.md (truth #22: "CommandSpec to_argv() matches requirement-specified commands") is fully resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/03-worktree-browser-git-and-rn-commands/03-05-SUMMARY.md`
</output>
